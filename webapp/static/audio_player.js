/* Placeholder — TTS pronunciation playback will live here.
   The audio button in index.html currently shows a "coming soon" toast
   (see swipe_logic.js). When the backend exposes a /api/tts endpoint we
   will wire it up from this file. */
